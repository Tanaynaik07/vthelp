<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Papers - VT Help</title>
    <link rel="stylesheet" href="/styles/main.css">
    <link rel="stylesheet" href="/styles/papers.css">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css">
</head>
<body>
    <div class="main-container">
        <button class="back-btn" onclick="window.location.href='/'">← Back to Subjects</button>
        <h1>Papers</h1>

        <% if (papers.length === 0) { %>
            <p>No papers found for this subject.</p>
        <% } else { %>
            <div class="folder-grid">
                <% papers.forEach(paper => { %>
                    <div class="folder-item">
                        <div class="folder-icon">
                            <i class="fas fa-folder"></i>
                        </div>
                        <div class="folder-content">
                            <h3><%= paper.subjectname %></h3>
                            <p class="year-sem"><%= paper.pyear %> (<%= paper.sem %>)</p>
                            <p class="exam-type">Exam Type: <%= paper.examtype %></p>
                            <p class="slots">Slots: <%= paper.slots.join('+') %></p>
                            <a href="<%= paper.plink %>" target="_blank" class="view-link">
                                <i class="fas fa-file-pdf"></i> View PDF
                            </a>
                        </div>
                    </div>
                <% }); %>
            </div>
        <% } %>
    </div>
</body>
</html>
